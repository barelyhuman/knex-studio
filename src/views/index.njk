<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    {{
      linkStyle("src/assets/styles.css") | safe
    }}
  </head>
  <body>
    <div class="container-fluid p-4">
      <div class="d-flex">
        <div class="flex-fill">
          <aside>{% include "./_partials/table.njk" %}</aside>
        </div>
        <div class="flex-fill px-3">
          <table id="database-datatable" class="mt-2 table table-bordered table-responsive">
            <thead>
              <tr>
                {% for col in columns %}
                <th class="col">{{ col.name }}</th>
                {% endfor %}
              </tr>
            </thead>
            <tbody>
              {% for dataRow in data %}
              <tr>
                {% for col in columns %}
                <td>{{ dataRow[col.name] }}</td>
                {% endfor %}
              </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
    {{ linkScript("src/assets/table-viewer.js") | safe }}
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
